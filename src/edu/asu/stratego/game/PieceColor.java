package edu.asu.stratego.game;

import java.io.Serializable;

/**
 * Colors to distinguish the players (armies) from one another.
 */
public enum PieceColor implements Serializable {
    RED,
    BLUE;
}